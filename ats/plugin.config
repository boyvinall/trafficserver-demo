# Plugin configuration for Apache Traffic Server
# Enables plugins needed for monitoring and caching

# stats_over_http - Exposes metrics for Prometheus
stats_over_http.so

# xdebug - Add debug headers (useful for development)
xdebug.so
